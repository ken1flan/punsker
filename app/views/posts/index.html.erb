<h1>Listing posts</h1>

<table>
  <thead>
    <tr>
      <th>Body text</th>
      <th>Emotion level</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.body_text %></td>
        <td><%= post.emotion_level %></td>
        <td><%= link_to 'Show', post %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post', new_post_path %>
